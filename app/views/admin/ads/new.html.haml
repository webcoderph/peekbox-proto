=form_for @ads,:url => admin_ads_path, :html => {:multipart => true} do |ad|
	%label{:for => "title"} Name:
	= ad.text_field :title
	%label{:for => "url"} Url:
	= ad.text_field :url
	%label{:for => "banner"} Banner Image:
	= ad.file_field :banner
	%label{:for => "location"} location:
	= select(:ads, :location, %w(home videos events user))
	= submit_tag "Add Ads"
