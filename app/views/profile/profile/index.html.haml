=render "profile_menu"
=render "administrator_menu"
%div
- if @user.profilepic?
	%script{:type => "text/javascript" , :charset => "utf-8"}
		jQuery(function($) {
		$('#profilepicture').Jcrop( {
		onChange: setCoords,
		onSelect:    setCoords,
		bgColor:     'black',
		bgOpacity:   .4,
		aspectRatio: 250 / 331	
		});
		});
		function setCoords(c) {
		var rx = 100/c.w;
		var ry = 100/c.h;
		var lw = $('#profilepicture').width();
		var lh = $('#profilepicture').height();
		= "var ratio = #{ @user.profile_geometry[:width] } / lw ";
		$("#cx").val(Math.round(c.x * ratio));
		$("#cy").val(Math.round(c.y * ratio));
		$("#w").val(Math.round(c.w * ratio));
		$("#h").val(Math.round(c.h * ratio));

		};
	%div{:style => "float:left;width:100%"}
		%div{:style => "float:left", :width => "700px", :height => "auto"}
			%h6
				Crop Profile Tool
			=image_tag @user.profilepic_url(:default_photo_limit).to_s, :id => "profilepicture"

		%div{:style => "float:right", :width => "700px", :height => "auto"}
			%h6
				Current Profile Picture
			=image_tag @user.profilepic_url(:profile_pic).to_s, :id => "onprofilePicture"

		%br{:style => "clear:both"}


%div{:style => "float:left", :width => "200px"}
	=form_for(@user, :url => save_profile_user_index_path, :html => {:multipart => true} ) do |u|
		%br
		= u.hidden_field :id
		%br
		%label{:for => "shoutout"} Shoutout:
		%br
		= u.text_field :shoutout
		%br
		%label{:for => "first_name"} First Name:
		%br
		= u.text_field :first_name
		%br
		%label{:for => "last_name"} Last Name:
		%br
		= u.text_field :last_name
		%br
		%label{:for => "location"} Location Name:
		%br
		= u.text_field :location
		%br
		%label{:for => "occupation"} Occupation:
		%br
		= u.text_field :occupation
		%br
		%label{:for => "sex"}  Sex:
		%br
		%label{:for => "sex_male"}  Male:
		= u.radio_button( :sex, "male")
		%label{:for => "sex_female"}  Female:
		= u.radio_button( :sex, "female")
		%br
		%label{:for => "website"} Website:
		%br
		= u.text_field :website
		%br
		%label{:for => "profilepic"} Profile Picture:
		%br
		= u.file_field :profilepic
		%br
		= hidden_field_tag(:cx)
		= hidden_field_tag(:cy)
		= hidden_field_tag(:h)
		= hidden_field_tag(:w)
		= submit_tag "Save Information!"


%br{:style => "clear:both"}

