-if @pictures.empty?
	%h1
		Pictures in "#{@album.title}" Album
	%p 
		No Picture Available
-else
	-@pictures.each do |pic|
		%p
			=pic.title
		%p
			=pic.description
		%p
			=link_to (image_tag image.image_url), image_path(image.id)
