=render "slideshow"
- unless @ad == nil
	%div.ads
		=link_to (image_tag @ad.banner_url(:banner_ad)), @ad.url
%br{:style => "clear:both"}
=render "featured_list"
%div#events-pagination
	=paginate @events
=render "non_featured"
%div#events-pagination
	=paginate @events
